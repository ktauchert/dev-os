# Optional frontend env (copy to frontend/.env.local)

# Real API when Lambda + API Gateway exist (Milestone 4+)
# VITE_API_BASE_URL=https://api.example.com

# Mock API via MSW (default: on in dev, off in production build)
# VITE_MOCK_API=true
# VITE_MOCK_API=false
